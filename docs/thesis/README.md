# Thesis Documents

Thesis title: **Development of Odin: A Personal Finance Management Application for Filipino Working Young Adults Using Random Forest, LSTM, and Isolation Forest**

> **Ground truth:** the authoritative versions of the thesis documents live in the Google Drive folder. Files here are working mirrors and may lag behind the Drive source — verify before citing.

## Structure

| Path | Contents |
| :--- | :--- |
| `topical-outline/` | Thesis topical outline (thesis structure ground truth). |
| `specifications/` | System specification (the main design contract). |
| `paper/` | Chapter drafts of the thesis paper. |
| `system/` | Product requirements, requirements engineering, screen descriptions, research notes, and preserved `(OLD)` copies. |

## Conventions

- Every thesis document carries a JSON `## Metadata` block (`document-type`, `version`, `date`, `authors`).
- Versioned from the Drive source; the repo copy is a mirror.
- Historical superseded files are kept under `system/` with an `(OLD)` marker.
